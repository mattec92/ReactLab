import React from 'react';

let BlogEntryHeader = React.createClass({
    render() {
        const styles = {
            container: {
            }
        };

        return (
            <div
                style={styles.container}>
                This is the blog entry header.
            </div>
        );
    }
});

export default BlogEntryHeader;